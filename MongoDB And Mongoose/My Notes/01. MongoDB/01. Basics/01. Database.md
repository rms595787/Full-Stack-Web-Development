# What is Database

An organised collection of data.  
A method to manipulate and access the data.

# MongoDB

MongoDB is a `NoSQL database` that stores data in flexible, JSON-like documents, making it easy to scale and handle large volumes of unstructured data.

# MongoDB Structure

MongoDB Stores data in `collections` and `documents`.

A collection is a group of MongoDB documents  
A document is a set of `key-value` pairs.

Example  
Here we want to store Data of three users (Raju, Sham, Baburao)  
<img src="./_images/Example 1 Database.png" alt="Example" width="600">

We are going to create three documents to store data of these three users  
<img src="./_images/Example 1 img2 Database.png" alt="Example" width="600">

This is how a `JSON` file looks like  
<img src="./_images/Example 1 img3 Database.png" alt="Example" width="600">

## Important Terms

- Collection: A container for documents (like a table).
- Document: A JSON-like record within a collection (like a row).
- Field: A key-value pair within a document (like a column).

# Brief History and Backgroun of MongoDB

- MongoDb was first released in `2009`
- It was developed by a `company` called `10gen`, which later rebranded as `MongoDB, Inc.`
