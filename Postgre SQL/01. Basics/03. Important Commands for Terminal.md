# Important command for terminal (mac) users

1. cmd + l

   clears the terminal

2. \l

   lists all databases

3. \c database_Name

   In SQL we use `use keyword`, `use database_Name` to switch between databases

   But in postgres we use `connect keyword represented as \c database_name`
