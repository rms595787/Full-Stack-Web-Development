const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>
        1. JSX must return a single parent element. <br />
        2. JSX elements must be properly closed. <br />
        3. JSX attributes are written using camelCase (e.g., `className` instead
        of `class`, `htmlFor` instead of `for` inside `label` in `form`).
      </p>
    </div>
  );
};

export default JSXRules;
