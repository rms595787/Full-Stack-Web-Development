# Components

Components are independent & reusable bits of code. They serve the same purpose as JS functions, but work in isolation and return HTML

# Separation of Concern

creates multiple function which returns html, css and js code

- creates everything in separate components like
- spearate component for NavBar, Hero, Timer, Calender, Cards, Menu, Search etc.

# Things to keep in my mind while creating a Component

- follow naming convention like this for any function creation -> first letter should be upper case
- you can create component using both ways
- - by using function keyword to create function
- - by using arrow funtion
- anytime you are working with component -> you must return some sort of html from that component
