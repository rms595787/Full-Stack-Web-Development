// 1. Create array with random numbers.
// 2. Map over each item in that array, & multiply each item by 10

let num = [12, 4, 53, 67, 23, 1];
let array = num.map((num) => num * 10);
console.log(array);
